"""Детерминированная проверка кандидатов на запись.

Две проверки: корректность YAML и границы правок — кандидат может менять
только файлы и ключи из подтверждённого плана, выход за них отклоняется.

Пакет не зависит от остальных модулей: код, который проверяет результат,
не переиспользует код, этот результат порождающий.
"""
