# release_promotion_agent — Бэклог и реестр открытых вопросов

> Порядок фиксирован — не перескакивать. Сделанное отмечать здесь же тем же коммитом.
>
> Конкретные адреса, идентификаторы задач и имена внутренних систем сюда не
> выносятся: они живут в конфигурации сессии и в окружении.

## Где что лежит

Имена модулей в этом файле и в docs/01 — архитектурные. После объединения
веток часть из них переехала; ниже соответствие, чтобы искать по коду.

| В документах | В коде этой ветки |
|---|---|
| `core/orchestrator.py` | `agents/release_agent.py` |
| `sourcecontrol/client.py` | `tools/git_tool.py` |
| `sourcecontrol/models.py` | `tools/models.py` |
| `audit/chain.py` | `core/audit_chain.py` |
| `audit/logging_setup.py` | `core/logging_setup.py` |
| `audit/redaction.py` | `core/redaction.py` |
| `analyzers/branch_diff.py` | без изменений |

## Спринт 0 — Скелет проекта (не заблокирован) — ✅ сделан

- [x] Структура пакетов `release_promotion_agent/` строго по docs/01; `pyproject.toml` (Python 3.11+).
- [x] Зависимости: httpx, pydantic>=2, ruamel.yaml, typer, rich, gigachat.
      Dev: pytest, pytest-cov, ruff, import-linter, pre-commit.
- [x] `config.py`: секреты только из окружения, fail-fast, allowlist авторизованных пользователей.
- [x] JSON-логирование: в каждой записи `session_id` и `user_id`.
- [x] Контракты import-linter: `validation` ни от кого не зависит, `core` ни от кого не зависит,
      рабочие модули не вызывают друг друга напрямую, к `validation` обращается только `llm`.
- [x] Dockerfile (python:3.11-slim), `.env.example`, `.gitignore`.
- **DoD:** `pytest` зелёный на пустом наборе, `ruff` и import-linter в pre-commit, контейнер собирается.

## Спринт 1 — Состояние сессии (не заблокирован) — ✅ сделан

> Постоянное хранилище сознательно отложено. Состояние живёт в памяти процесса:
> сессия может стоять часами, но перезапуск её не переживает. Работа с состоянием
> идёт через интерфейс хранилища, поэтому подключить постоянное можно будет одной
> реализацией, не трогая оркестратор.

- [x] `core/fsm.py`: состояния сессии и допустимые переходы из docs/01.
- [x] `core/context.py`: SessionContext — идентификаторы и параметры, передаётся явно.
- [x] `core/session.py`: хранилище сессии и задач по репозиториям в памяти процесса.
- **DoD:** переходы FSM покрыты тестами, недопустимый переход отклоняется.

## Спринт 2 — SourceControl read-only + Branch Diff Analyzer — ✅ сделан

- [x] `sourcecontrol/`: httpx-клиент v3, часть чтения (`compare`, `files`, `repos`), таймауты, 3 ретрая.
      ⚠️ Пути эндпоинтов, схемы ответов и Bearer-аутентификация — рабочие допущения,
      изолированы в клиенте (вопрос №10). `merge()` отсутствует, тест это фиксирует.
- [x] `analyzers/branch_diff.py`: нормализация diff релизной ветки к базовой в структуру
      «файл → ключ → было/стало», ruamel.yaml round-trip и stable_hash (звено diff_hash аудита).
- [x] Fixtures: синтетические ответы API v3 и примеры YAML-конфигов контроллеров.
- **DoD:** анализатор на fixtures выдаёт стабильную нормализованную структуру; живых вызовов в тестах нет.
- ⚠️ Живой вызов API — только после закрытия вопросов №4–5 (хост, аутентификация).

## Спринт 3 — Reconciliation + Validation (не заблокирован) — ✅ сделан

- [x] Внутренняя структура «намерения» (наша, не формат плана установки) — `analyzers/intent.py`.
- [x] `analyzers/reconciliation.py`: детерминированная сверка ФАКТ ↔ НАМЕРЕНИЕ,
      отчёт расхождений (FACT_ONLY / INTENT_ONLY / VALUE_MISMATCH / FACT_UNPARSED).
      Ничего не дорешивает.
- [x] `validation/`: pydantic-модели кандидата, YAML-валидация, контроль границ плана
      (собственное сравнение, без импорта analyzers — validation ни от кого не зависит).
- [x] Fixtures: пары «diff и намерение → эталонный отчёт»; новый кейс — новый каталог,
      код не меняется. Тестовая корзина начата.
- **DoD:** сверка полностью детерминирована (одинаковый вход → одинаковый отчёт);
  контроль границ режет изменение вне плана жёстким отказом.

## Спринт 4 — Оркестратор happy-path + CLI (не заблокирован) — ✅ сделан

- [x] `core/orchestrator.py`: узкий ReAct-скелет, цикл по репозиториям, стоп-условия из docs/01
      (3 попытки / идентичная ошибка дважды / деградация модели). PlanEngine и HumanInterface —
      Protocol: `llm/` и `sberchat_bot/` подключатся адаптерами, ядро не изменится.
- [x] `sourcecontrol/` часть записи (`branches`, `commits`, `pulls`, `comments`) — на fixtures.
      `merge()` не существует (закреплено тестом).
- [x] `cli/`: Typer и Rich — check-config, demo, chat. Полный `run` — после разблокировки
      `llm/` (№2) и живых вызовов (№4–5).
- [x] `audit/`: JSON-события, редакция секретов, цепочка хэшей и реконструкция по репозиторию.
- **DoD:** happy-path на моках проходит от параметров до «PR открыт» с двумя чекпоинтами;
  в аудите восстанавливается полная цепочка.

## ЗАБЛОКИРОВАНО — не начинать до закрытия вопроса

| Модуль | Чем заблокирован |
|---|---|
| `analyzers/deploy_plan.py` | ❓ №2 — формат плана установки (главный технический блокер) |
| `llm/` промпты (клиент и заглушки инструментов можно раньше) | ❓ №2 — формат плана установки |
| `sberchat_bot/` | ❓ №1 — механика bot API (консультация) |
| Живые вызовы SourceControl | ❓ №4, №5 — аутентификация и хост |

## Реестр открытых вопросов

| № | Вопрос | Зачем | У кого | Блокирует | До ответа |
|---|---|---|---|---|---|
| 1 | Bot API чата: webhook или polling, SDK, регистрация бота, команды, кнопки, лимиты | Модуль `sberchat_bot/` | владельцы сервиса | `sberchat_bot/` | Спринты 0–4; ядро транспорт-агностично |
| 2 | 🟡 **Частично закрыт.** Структура страницы релиза разобрана и описана в [02_INTEGRATIONS.md](02_INTEGRATIONS.md). Остаток: исходник страницы в storage format (XML) для точных имён макросов | Парсер `deploy_plan.py` | Confluence: страница релиза → просмотр исходного кода | Точную разметку парсера | Модель НАМЕРЕНИЯ и структура разделов уже описаны |
| 3 | 🟡 **Частично закрыт:** релизная ветка вида `release/<версия>`; стенд — это ФАЙЛ, общие параметры в общем файле. Остаток — см. №11 | Точные пути для Branch Diff Analyzer | инженер сопровождения | Точную настройку, не код | Пути в конфигурацию, не в код |
| 4 | Аутентификация SourceControl API (API-ключ технической учётной записи) | Живые вызовы | внутренний портал документации | Живые вызовы | Всё на fixtures |
| 5 | 🟡 Хост контура известен. Остаток: подтвердить, что API отвечает на отдельном API-домене | Живые вызовы | владелец API | Живые вызовы | `base_url` из окружения |
| 6 | 🟡 Известен один репозиторий конфигов. Остаток: полный список репозиториев и правило «хаб → репозиторий» | Цикл по репозиториям | инженер сопровождения | Конфигурацию, не код | Соответствие из конфигурационного файла |
| 7 | Docker на рабочей машине — политика информационной безопасности | Легальность контейнера | информационная безопасность | Ничего в коде | Разработка идёт |
| 8 | Процедура валидации модели для локального инструмента · размер модели · FOSS во внутреннем зеркале · регистрация обращений | Путь в промышленную среду | владельцы процесса и бенчмарки | Ничего на MVP | Тестовая корзина копится |
| 9 | ❓ Схемы архитектуры в drawio ещё не добавлены в репозиторий | Сверка схем с docs/01 | инженер сопровождения | Ничего в коде | Источник истины — docs/01; при появлении схем сверить, схемы побеждают |
| 10 | 🟡 **Пути закрыты:** адресация репозитория тройкой, PR по `index`, запись файлов пофайловая, слияние в API есть — не реализуем. Остаток: схемы тел запросов и ответов | Соответствие «ответ API → DTO» | внутренний Swagger | Живые вызовы (вместе с №4–5) | Допущения по телам изолированы в клиенте и fixtures |
| 11 | ✅ **Закрыт.** Структура репозитория конфигов: три каталога с набором стендов плюс общий файл. Расширения смешанные (`.yaml` и `.yml`) | Куда агент пишет изменения | инженер сопровождения | — | Целевые файлы — из конфигурации сессии, не из кода |
| 12 | ❓ Плейсхолдеры в плане установки (адреса брокеров, шаблон по имени хаба) — откуда инженер берёт реальные значения | Может ли агент подставить значение сам или обязан спросить | инженер сопровождения | Полноту плана | Плейсхолдер — позиция плана с пометкой «нужно значение от человека», жёсткая остановка в цикле 1 |
| 13 | ❓ Правило «строка плана установки → репозиторий и файл»: заголовок потока перечисляет хабы, но как из хаба получить репозиторий и какой файл править | Ядро сопоставления НАМЕРЕНИЯ с файлами | инженер сопровождения | Автоматический выбор целей | Пока цель указывает человек в параметрах сессии |
| 14 | 🟡 **Состав веток известен:** постоянные `master` и `psi`, релизные вида `release/<версия>`. Остаток: какую из двух брать за базу в конкретном релизе | Branch Diff Analyzer: с чем сравнивать релизную | инженер сопровождения | Правильность ФАКТА | `base_ref` задаётся параметром сессии |
| 15 | ❓ Значение `{tenant}` для нашего контура: в путях API тенант есть, в ссылке UI его не видно | Все вызовы API — путь без тенанта не построить | внутренний Swagger или владелец API | Живые вызовы | `tenant` — параметр `RepoRef` из конфигурации сессии |
| 16 | ❓ Расхождение в имени релизной ветки между планом установки и деревом веток (уточнение патч-версии; возможно, это тег) | Точное имя ветки для diff и PR | инженер сопровождения | Точность, не код | Имя ветки — параметр сессии |

## Правило

Каждый новый факт о внутренних системах в коде или документации — ✅ (с источником)
или ❓ (сюда в реестр). Выдуманных фактов не существует. Закрыл вопрос — перенеси
ответ в [02_INTEGRATIONS.md](02_INTEGRATIONS.md) с пометкой ✅.

Конкретные адреса, идентификаторы задач, имена хабов, контуров и внутренних систем
в документацию не переносятся — только в конфигурацию.
