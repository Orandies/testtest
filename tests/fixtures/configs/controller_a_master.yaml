# Синтетический конфиг контроллера — версия master/PSI (base)
server:
  port: 8080
  timeout-seconds: 30
integration:
  retries: 3
  endpoints:
    - https://service-a.invalid
    - https://service-b.invalid
features:
  new-billing: false
logging:
  level: INFO
